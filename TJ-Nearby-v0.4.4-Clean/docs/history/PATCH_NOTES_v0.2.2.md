# TJ Nearby v0.2.2

- Bundle `certifi` CA certificate explicitly for py2app.
- Resolve the CA bundle safely in normal and frozen app execution.
- Show a macOS notification after an explicit **Check now** action.
- Export exception type, missing filename, and full traceback in diagnostics.
- Use absolute paths for macOS `open` and `osascript` utilities.
